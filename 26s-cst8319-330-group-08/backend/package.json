{
  "name": "homeboost-backend",
  "version": "1.0.0",
  "description": "HomeBoost client website backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "npm run migrate && nodemon src/server.js",
    "start": "npm run migrate && node src/server.js",
    "check": "node scripts/check-syntax.js",
    "migrate": "node scripts/migrate.js",
    "test": "node scripts/run-tests.js && npm run check",
    "test:integration": "node scripts/run-integration-tests.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.1",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "mysql2": "^3.15.3",
    "stripe": "^22.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
